# ═══════════════════════════════════════════════════════════════
# Temple Stuart — Environment Variables
# Copy this file to .env.local and fill in your values
# ═══════════════════════════════════════════════════════════════

# ─── DATABASE (required) ──────────────────────────────────────
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/temple_stuart

# ─── AUTHENTICATION (required) ────────────────────────────────
# Secret for signing JWT tokens
JWT_SECRET=your-jwt-secret-here

# NextAuth.js secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=your-nextauth-secret-here

# Base URL for NextAuth
NEXTAUTH_URL=http://localhost:3000

# ─── PLAID — Banking Integration (required) ───────────────────
# Plaid API credentials — app uses production environment
PLAID_CLIENT_ID=your-plaid-client-id
PLAID_SECRET=your-plaid-secret

# ─── ANTHROPIC — Claude AI (required for trading features) ────
# Powers market briefs and per-strategy analysis
ANTHROPIC_API_KEY=your-anthropic-api-key

# ─── FINNHUB — News & Analyst Ratings (optional) ─────────────
# Free tier works — provides headlines and ratings per ticker
FINNHUB_API_KEY=your-finnhub-api-key

# ─── OPENAI (optional) ───────────────────────────────────────
# Used for meal planning, cart planning, spending insights
OPENAI_API_KEY=your-openai-api-key

# ─── XAI GROK — Trip AI Analysis (optional) ──────────────────
# Powers sentiment scoring, fit scoring, warnings for trip places
XAI_API_KEY=your-xai-api-key

# ─── DUFFEL — Flight Booking (optional) ──────────────────────
# GDS access for real-time flight search and booking
DUFFEL_API_TOKEN=your-duffel-api-token

# ─── GOOGLE PLACES — Location Intelligence (optional) ────────
# Used for trip destination search and place details
GOOGLE_PLACES_API_KEY=your-google-places-api-key

# ─── AMADEUS — Hotel Search (optional) ───────────────────────
AMADEUS_API_KEY=your-amadeus-api-key
AMADEUS_API_SECRET=your-amadeus-api-secret

# ─── YELP (optional) ─────────────────────────────────────────
# Used for business verification
YELP_API_KEY=your-yelp-api-key

# ─── STRIPE — Payments & Subscriptions (optional) ────────────
STRIPE_SECRET_KEY=your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret
STRIPE_PRO_PRICE_ID=your-stripe-pro-price-id
STRIPE_PRO_PLUS_PRICE_ID=your-stripe-pro-plus-price-id

# ─── OAUTH PROVIDERS (optional) ──────────────────────────────
# For NextAuth social login
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ─── APP CONFIG ──────────────────────────────────────────────
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ─── ADMIN (optional) ────────────────────────────────────────
ADMIN_PASSWORD=your-admin-password

# ─── CRON (optional) ─────────────────────────────────────────
# Secret for cron job authentication
CRON_SECRET=your-cron-secret
